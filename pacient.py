
from pandas import DataFrame, read_csv

class Pacient:

    def __init__(self, name, sensor=, queue):
        self.name = name

    def readData():

    def data(self):

        with self.sensor  as resquest:
            response = resquest.get()
            self.queue.put(response)
        
